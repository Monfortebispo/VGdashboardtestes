{
  "name": "vg-dashboard-operacoes",
  "version": "35.6.0",
  "private": true,
  "description": "VG Operations v35.6: aplicação operacional com migração arquitetural progressiva",
  "scripts": {
    "test": "node tests/run-tests.js",
    "validate": "node tests/run-tests.js",
    "modern:dev": "vite --config vite.config.ts",
    "modern:build": "tsc --noEmit && vite build --config vite.config.ts",
    "modern:preview": "vite preview --config vite.config.ts",
    "modern:deploy-preview": "npm test && npm run modern:build && node scripts/build-modern-preview.js && node scripts/defer-modern-preview-runtime.js && node scripts/prioritize-login-auth.js && node scripts/patch-service-worker-performance.js && node scripts/inject-central-operations-home-cleanup.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.10"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
